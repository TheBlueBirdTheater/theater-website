Reserved for build-time-optimized, ESM-imported images (e.g. via Astro's
`<Image>` component), resolved through the `@assets/*` path alias. Currently
empty — distinct from `public/images/`, which holds the string-path
placeholder images referenced directly from `src/data/*.ts`.
