---
title: "About"
description: "The story of Orangeburg Part-Time Players and the historic Blue Bird Theatre."
layout: "@layouts/DefaultLayout.astro"
---

import { buildingHistory } from '@data/history';
import { boardMembers, artisticStaff } from '@data/boardAndStaff';

<div class="mx-auto max-w-3xl px-4 py-16">

# About Orangeburg Part-Time Players

{buildingHistory.intro}

## Our History

{buildingHistory.milestones.map((milestone) => (
  <div class="mt-6">
    <p class="font-heading font-semibold">{milestone.year} — {milestone.heading}</p>
    <p>{milestone.body}</p>
  </div>
))}

## Artistic Staff

{artisticStaff.map((person) => (
  <p><strong>{person.name}</strong> — {person.role}</p>
))}

## Board of Directors

{boardMembers.map((person) => (
  <p><strong>{person.name}</strong> — {person.role}</p>
))}

</div>
